{
  "name": "cv-generator-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "scripts": {
    "build:packages": "pnpm --filter \"@cv-generator/*\" run build",
    "start:cli": "pnpm run build:packages && pnpm --filter @cv-generator/cli run start",
    "start:cli:cache": "pnpm run build:packages && pnpm --filter @cv-generator/cli run start:cache",
    "dev:cli": "pnpm --filter @cv-generator/cli run dev"
  }
}
